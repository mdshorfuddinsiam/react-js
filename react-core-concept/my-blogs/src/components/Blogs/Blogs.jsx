import React from 'react';

// const Blogs = (props) => {
//     console.log(props);
const Blogs = ({blogs}) => {
    // console.log(blogs);

    return (
        <div>
            
        </div>
    );
};

export default Blogs;
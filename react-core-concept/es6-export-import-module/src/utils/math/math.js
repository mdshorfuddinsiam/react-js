const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const multiplication = (a, b) => a * b;
const divide = (a, b) => a / b;

// ----- export (concept) -------
// const name = 'siam';
// const profession = 'programmer';
// // const person = {name: name, profession: profession}
// const person = {name, profession}
// console.log(person);

// Note: (property) r (variable) name same hole (variable) use krlei ta (object) er (property & value) heshabe kaj kore
// -------------------------------

export {add, sub, multiplication as mul, divide}

